What is the purpose of using sessions?

Sessions are used to save authentication information, so there is no need to re-enter credentials on every new request the client makes to the server.

 What does bcrypt do to help us store passwords in a secure manner.

Bcrypt Hashes passwords before saving them to the database.

 What does bcrypt do to slow down attackers? 

It adds time to security algorithm to produce what is known as a key derivation function.

 What are the three parts of the JSON Web Token? Header, payload, signature

